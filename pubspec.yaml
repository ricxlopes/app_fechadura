name: app_autenticador_fechadura
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  local_auth: ^1.1.7
  firebase_core: ^1.3.0
  http: any
  mqtt_client: ^9.6.1
  provider: ^6.0.1
  flutter_switch: ^0.3.2
  list_tile_switch: ^1.0.0
  alan_voice: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/full-bloom.png
    - assets/door.png
    - assets/gif_door.gif
    - assets/garagem.png
  uses-material-design: true